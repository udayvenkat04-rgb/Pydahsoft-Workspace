# Landing background template

The landing page uses the attached pale-green layered-wave theme through the `landing-background` styles in `src/App.css`.

Place the original PNG asset in this folder when its local file is available.
